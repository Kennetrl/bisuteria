---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProductCard from '../components/ProductCard.astro';
import { SITE, WA_MESSAGES } from '../config/site.ts';
import type { ImageMetadata } from 'astro';

const imageModules = import.meta.glob<{ default: ImageMetadata }>(
  '../assets/collares/*.jpg',
  { eager: true }
);

const images = Object.entries(imageModules)
  .sort(([a], [b]) => a.localeCompare(b))
  .map(([path, mod]) => ({
    src: mod.default,
    name: path.split('/').pop()?.replace('.jpg', '') || '',
  }));

const siteName = SITE.name || 'Joyería Salomé';
const title = `Collares de Acero Inoxidable en ${SITE.city}`;
const description = `Collares de acero inoxidable quirúrgico 316L en ${SITE.city}, ${SITE.country}. ${images.length} diseños que no se oxidan y son resistentes al agua. Joyas de buena calidad que duran años.`;

const schema = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": `Collares de Acero Inoxidable | ${siteName}`,
  "description": description,
  "url": `${SITE.url}/collares`,
  "numberOfItems": images.length,
  "itemListElement": images.map((_, i) => ({
    "@type": "ListItem",
    "position": i + 1,
    "url": `${SITE.url}/collares#producto-${i + 1}`
  }))
};
---

<BaseLayout
  title={title}
  description={description}
  canonical="/collares"
  schema={schema}
  keywords="collares de acero inoxidable, collares que no se oxidan, collares resistentes al agua, collares de buena calidad"
>

  <div class="page-header">
    <h1>Collares de Acero Inoxidable</h1>
    <p>Acero Quirúrgico 316L · No se oxidan · Resistentes al agua · {images.length} diseños</p>
  </div>

  <nav class="breadcrumb" aria-label="Ruta de navegación">
    <a href="/">Inicio</a>
    <span aria-hidden="true">›</span>
    <span aria-current="page">Collares</span>
  </nav>

  <div class="seo-intro">
    <p>
      Nuestros collares de acero inoxidable quirúrgico 316L no se oxidan ni pierden el brillo con el tiempo.
      Úsalos a diario, en la piscina o bajo la lluvia — están hechos para durar años sin desgastarse.
      El accesorio perfecto para elevar cualquier outfit con estilo y elegancia.
    </p>
  </div>

  <section class="section" aria-labelledby="collares-heading">
    <h2 class="section__title" id="collares-heading">Catálogo de Collares</h2>
    <div class="section__divider"></div>
    <div class="products-grid">
      {images.map((item, i) => (
        <ProductCard
          image={item.src}
          alt={`Collar ${item.name} de acero inoxidable - no se oxida - ${siteName} ${SITE.city}`}
          name={item.name}
          waMessage={WA_MESSAGES.collares}
          index={i}
        />
      ))}
    </div>
  </section>

</BaseLayout>

<style>
  .seo-intro {
    max-width: 860px;
    margin: 0 auto;
    padding: 32px 24px 0;
  }
  .seo-intro p {
    color: var(--color-text-light);
    line-height: 1.75;
    font-size: 1rem;
  }
</style>
